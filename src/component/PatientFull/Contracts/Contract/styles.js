import {templates} from 'styles';

export default {

    container: {
        margin: "8px 0 8px 0",
        flexWrap: "wrap",
        display: "flex"
    },

    expired: {
        color: "#ddd"
    },

    number: {
        color: templates.colors.border,
        margin: "0 8px 0 0"
    },

    period: {
        fontStyle: "italic"
    }

}
